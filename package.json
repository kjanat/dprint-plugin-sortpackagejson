{
	"name": "workspace-root",
	"version": "0.0.0",
	"workspaces": [
		"deployment/npm"
	],
	"scripts": {
		"bd": "bun run --filter '*' bd",
		"build": "bun run --filter '*' build",
		"prepack": "bun run --filter '*' prepack",
		"test": "bun run --filter '*' test"
	},
	"devDependencies": {
		"sort-package-json": "3.6.1"
	},
	"volta": {
		"node": "25.9.0",
		"npm": "11.13.0"
	}
}
